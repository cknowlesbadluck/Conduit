# Conduit / portfolio audit — 2026-09-25 18:03 EDT

Single current audit. Hourly PORTFOLIO-AUDIT-* files are superseded.

## Live

- `https://conduit-feco.onrender.com/health` 200
- `https://conduit-feco.onrender.com/ready` 200 `{version:"0.8.0", persistence:"postgres"}`
- This connector bound as `grok`. bindingConflict=false. Scopes read+write.

## Main

`2eb338bd` after #128. Closed #129 (duplicate 17:00 docs).

## Open PRs — freeze rules stand

- #119 DRAFT KEEP RED — SSE admission. Do not merge.
- #120 DRAFT KEEP RED — ordered migrations. Do not merge.
- #125 keyset pagination — leave open. Merge only after rebase onto current main and required verify + postgres-coordination are green. This session only observed CodeRabbit status=success, which is not enough.

## Hygiene

Leftover `docs/portfolio-audit-*` and `codex/*` branches remain. This connector has no delete-ref tool. Owner must prune after PRs close.
Do not hard-code Resonance into Conduit core.
