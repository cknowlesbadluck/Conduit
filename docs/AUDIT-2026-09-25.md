# Conduit / portfolio audit — 2026-09-26 09:00 EDT

Single current audit. Hourly PORTFOLIO-AUDIT-* files are forbidden.

## Live

- `https://conduit-feco.onrender.com/health` 200
- `https://conduit-feco.onrender.com/ready` 200 `{version:"0.8.0", persistence:"postgres"}`
- Diagnostics: health/PRM/AS/JWKS/scopeParity/CIMD/DCR all ok
- This connector bound as `grok`. bindingConflict=false. Scopes read+write.
- `activity_prune` removed 0.
- Integrations: github configured, render configured, supabase unconfigured.

## Main

`5659a495` after #125 keyset pagination merged 2026-09-26T12:09:14Z.
Required CI on hygiene PRs: verify + postgres-coordination. Workers Builds is not required.

## Open PRs — freeze rules stand

- #119 DRAFT KEEP RED — SSE admission. Do not merge.
- #120 DRAFT KEEP RED — ordered migrations. Do not merge. Landing it without a start-path runner takes postgres coordination down.
- #125 — DONE / merged.
- #137 — in-place 09:00 docs. Merge only if required checks stay green after this push.

## Hygiene

Leftover `docs/portfolio-audit-*`, `docs/hygiene-*`, `codex/*`, `bolt/*`, and `release/0.8.0` branches remain. This connector has no delete-ref tool. Owner must prune after PRs close.
Do not hard-code Resonance into Conduit core.
