# Conduit / portfolio audit — 2026-09-26 13:01 EDT

Single current audit. Hourly PORTFOLIO-AUDIT-* files are forbidden.

## Live

- `https://conduit-feco.onrender.com/health` 200
- `https://conduit-feco.onrender.com/ready` 200 `{version:"0.8.0", persistence:"postgres"}`
- HTTP `GET /diagnostics` 404. MCP `conduit_diagnostics` is green (health/PRM/AS/JWKS/scopeParity/CIMD/DCR).
- This connector bound as `grok`. bindingConflict=false. Scopes read+write.
- Integrations: github configured, render configured, supabase unconfigured.

## Main

`dacff1d3` after prior docs squash-merge. Product line still `#125` keyset pagination merged 2026-09-26T12:09:14Z.
Required CI on hygiene PRs: verify + postgres-coordination. Workers Builds is not required.

## Open PRs — freeze rules stand

- #119 DRAFT KEEP RED — SSE admission. Do not merge. Base still `7c3007a` (behind main).
- #120 DRAFT KEEP RED — ordered migrations. Do not merge. Landing it without a start-path runner takes postgres coordination down.
- #125 — DONE / merged.
- #139 — 12:00/13:01 in-place hygiene. Merge only if required checks green.

## Hygiene

Leftover `docs/hygiene-0805`, `docs/hygiene-1106`, `codex/add-paginated-store-methods-with-cursor-decoding`, other `codex/*`, and `release/0.8.0` branches remain. This connector has no delete-ref tool. Owner must prune after PRs close.
Do not hard-code Resonance into Conduit core.
