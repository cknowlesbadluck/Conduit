# Conduit / portfolio audit — 2026-09-26 03:00 EDT

Single current audit. Hourly PORTFOLIO-AUDIT-* files are superseded and removed on this branch.

## Live

- `https://conduit-feco.onrender.com/health` 200
- `https://conduit-feco.onrender.com/ready` 200 `{version:"0.8.0", persistence:"postgres"}`
- Diagnostics: health/PRM/AS/JWKS/scopeParity/CIMD/DCR all ok
- This connector bound as `grok`. bindingConflict=false. Scopes read+write.

## Main

`9f3c4714` after #130. This PR deletes leftover hourly audit files and adds a docs-hygiene workflow.

## Open PRs — freeze rules stand

- #119 DRAFT KEEP RED — SSE admission. Do not merge.
- #120 DRAFT KEEP RED — ordered migrations. Do not merge. Landing it without a start-path runner takes postgres coordination down.
- #125 keyset pagination — leave open. Merge only after rebase onto current main and required verify + postgres-coordination are green. Workers Builds is not a required check.

## Hygiene

Leftover `docs/portfolio-audit-*` and `codex/*` branches remain. This connector has no delete-ref tool. Owner must prune after PRs close.
Do not hard-code Resonance into Conduit core.
